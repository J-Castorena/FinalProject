import { DateNightDiscussionBoard } from './date-night-discussion-board';

describe('DateNightDiscussionBoard', () => {
  it('should create an instance', () => {
    expect(new DateNightDiscussionBoard()).toBeTruthy();
  });
});
